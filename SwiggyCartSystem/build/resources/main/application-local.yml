spring:
  jpa:
    hibernate:
      ddl-auto: validate
  datasource:
    url: jdbc:postgresql://localhost:5433/swiggy
    username: postgres
    password: postgres
  application:
    name: SwiggyCartSystem
logging:
  level:
    org:
      springframework: DEBUG
